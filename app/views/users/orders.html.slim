div class="container"
  table class="table"
    thead
      tr
        th scope="col" Order number
        th scope="col" Created at
        th scope="col" Price
        th scope="col" Action
    tbody
      - @orders.each do |order|
        tr
          td = order.id
          td = order.created_at.strftime("%m/%d/%Y %H:%M")
          td = order.total_price
          td = link_to "Подробнее", order_path(order)
